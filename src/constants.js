const Theme = Object.freeze({
  DEFAULT: 0,
  LIGHT: 1,
})

const BaseBehavior = Object.freeze({
  OWNERSHIP: 0,
  COMMUNICATE: 1,
  GIVE: 2,
  PEOPLE: 3,
  GROWTH: 4,
})

export { Theme, BaseBehavior }
